#include <bits/stdc++.h>
using namespace std;

// Goods on the Shelf

int main() {

    int T;
    cin >> T;

    while (T--) {
        int n;
        cin >> n;
        vector<int> v(n);
        for (int i = 0; i < n; i++) {
            cin >> v[i];
        }
        vector<int> vv=v;
        sort(vv.begin(), vv.end());
        unordered_map<int, int> mp;
        for(int i=0;i<n;i++){

        }
    }
}